name: Superseded fail-closed actor dispatch branch

on:
  push:
    branches:
      - fix/fail-closed-actor-dispatch
    paths:
      - .github/workflows/apply-fail-closed-actor-dispatch.yml

concurrency:
  group: apply-fail-closed-actor-dispatch
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by the existing stacked implementation in PR #269; no product changes will be applied from this branch."
