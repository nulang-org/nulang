name: Apply Value Safety Migration Current

on:
  push:
    branches:
      - fix/value-safety-current-main-v2

permissions:
  contents: write

jobs:
  apply-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/value-safety-current-main-v2
          fetch-depth: 0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install feature dependencies
        run: sudo apt-get update && sudo apt-get install -y python3-dev

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Apply fail-closed migration
        run: python3 scripts/apply_value_safety.py

      - name: Format
        run: cargo fmt --all

      - name: Compile all features
        run: cargo check --all-features

      - name: Clippy correctness gate
        run: cargo clippy --all-targets --all-features -- -D clippy::correctness

      - name: Reject whitespace damage
        run: git diff --check

      - name: Remove one-shot machinery
        run: |
          rm .github/workflows/apply-value-safety-current.yml
          rm scripts/apply_value_safety.py

      - name: Commit verified implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -s -m "fix(vm): harden raw Value construction boundaries"
          git push origin HEAD:fix/value-safety-current-main-v2
