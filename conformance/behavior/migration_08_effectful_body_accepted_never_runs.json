{"exit_code":5,"stdout":"","stderr":"`perform` is forbidden by RFC 0008 migration purity","proves":"RFC 0008 migration bodies are now compiler-enforced pure replay transforms: an effectful state migration containing `perform IO.print(42)` fails at the effect-checking boundary instead of compiling and being silently dropped before runtime."}
