actor IntPinger {
    behavior ping(x: Int) { x + 1 }
}

actor StringPinger {
    behavior ping(x: String) { x }
}

fn main() {
    let ints = spawn IntPinger {}
    send ints ping("wrong actor payload")
}
