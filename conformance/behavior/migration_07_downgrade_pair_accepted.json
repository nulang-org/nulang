{ "exit_code": 4, "stdout": "", "stderr": "RFC 0008 requires W == V + 1", "proves": "RFC 0008 migration transitions are now forward-only and adjacent. A version-2 entity that declares a 2 -> 1 downgrade contract is rejected during compiler artifact lowering instead of silently accepting an unusable migration." }
