let comp = fn(src) => fn(pos) => fn(len) => fn(left) => fn(min_prec) => fn(nr) => fn(env) => fn(elen) => (
    let no_left = 268435456 * 4096 in
    let pair = if left == no_left then (
        if pos >= len then ((nr * 65536)) + pos
        else 42
    ) else 0 in
    pair
) in
comp("hello")(10)(5)(268435456 * 4096)(-1)(8)("")(0)
