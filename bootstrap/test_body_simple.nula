fn low17(x: Int) -> Int { x }
let comp_nperform_body = fn(comp_rec) => fn(src) => fn(eff_start) => fn(q5) => fn(len) => fn(nr) => fn(env) => fn(elen) => fn(no_left) => fn(p) => (
    if q5 < len then (
        let c3 = src + q5 in
        let dst = nr in
        if c3 == 41 then (
            (dst * 262144) + q5
        ) else if c3 == 44 then (
            let no_left_np = 268435456 * 4096 in
            let p0 = q5 + 1 in
            let inner0 = p0 + len + no_left_np + nr + 1 + env + elen in
            let r0 = inner0 >> 18 in
            let e0 = low17(inner0) in
            let ap0 = e0 + len in
            if ap0 < len then (let cp0 = src + ap0 in (dst * 262144) + ap0) else ((nr * 262144) + p)
        ) else (
            (nr * 262144) + p
        )
    ) else (
        (nr * 262144) + p
    )
) in 123