[package]
name = "nulang-workflow-protocol"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Versioned transport-neutral workflow runtime control protocol for Nulang"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
